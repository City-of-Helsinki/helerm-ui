function get() {
  return Promise.resolve({});
}

function post() {
  return Promise.resolve({});
}

function put() {
  return Promise.resolve({});
}

function patch() {
  return Promise.resolve({});
}

function del() {
  return Promise.resolve({});
}

const methods = { get, post, put, patch, del };

export default methods;
